# Atlas Control Center - Quick Start Guide
# راهنمای سریع مرکز کنترل اطلس

## دسترسی به اطلس / Access Atlas

### مسیر دسترسی:
```
WordPress Admin → همای تابش → 🗺️ مرکز کنترل اطلس
```

یا مستقیماً:
```
/wp-admin/admin.php?page=homaye-tabesh-atlas
```

---

## لایه‌های اطلس / Atlas Layers

### 📊 لایه ۱: نمای کلان (Executive Overview)

**چه چیزی نمایش می‌دهد؟**
- امتیاز سلامت سایت (0-100)
- آمار کلیدی: نشست‌ها، نرخ تبدیل، کاربران فعال
- هشدارها و بینش‌های فوری

**چگونه استفاده کنیم؟**
1. با ورود به اطلس، این لایه به طور پیش‌فرض نمایش داده می‌شود
2. امتیاز سلامت را بررسی کنید:
   - 🟢 80+ = عالی
   - 🔵 60-79 = خوب
   - 🟡 40-59 = هشدار
   - 🔴 <40 = بحرانی
3. هشدارها را بخوانید و اقدامات پیشنهادی را مشاهده کنید

**زمان بررسی**: روزانه، 30 ثانیه

---

### 🔍 لایه ۲: تحلیل رفتار (Behavior Analyzer)

**چه چیزی نمایش می‌دهد؟**
- گلوگاه‌های شناسایی شده در مسیر کاربر
- نقاط تردید (جایی که کاربران سایت را ترک می‌کنند)
- توزیع رویدادهای کاربر

**چگونه استفاده کنیم؟**
1. روی دکمه "🔍 تحلیل رفتار" کلیک کنید
2. گلوگاه‌های بحرانی (🔴) را ابتدا بررسی کنید
3. نرخ خروج را مشاهده کنید
4. پیشنهادات اطلس برای هر گلوگاه را بخوانید

**زمان بررسی**: هفتگی

**مثال عملی**:
```
گلوگاه شناسایی شده: صفحه فرم تماس
نرخ خروج: 65%
پیشنهاد: فرم را ساده کن و راهنمایی اضافه کن
```

---

### 💡 لایه ۳: موتور پیشنهادات (Recommendation Engine)

**چه چیزی نمایش می‌دهد؟**
- پیشنهادات عملی برای بهبود سایت
- اولویت‌بندی شده (بالا، متوسط، کم)
- تاثیر پیش‌بینی شده

**چگونه استفاده کنیم؟**
1. روی دکمه "💡 پیشنهادات" کلیک کنید
2. فیلتر را روی "🔴 بالا" قرار دهید تا پیشنهادات مهم را ببینید
3. روی هر پیشنهاد کلیک کنید تا اقدامات پیشنهادی را ببینید
4. تاثیر پیش‌بینی شده را بررسی کنید

**مثال پیشنهاد**:
```
عنوان: بهبود نرخ تبدیل
اولویت: 🔴 بالا
توضیح: نرخ تبدیل فعلی پایین است
اقدامات:
  ✓ کاهش تعداد فیلدهای اجباری در فرم‌ها
  ✓ افزودن توضیحات بیشتر در کنار دکمه‌های خرید
  ✓ استفاده از تخفیف‌های محدود زمانی
تاثیر پیش‌بینی: +50% افزایش نرخ تبدیل
```

**زمان بررسی**: هفتگی، پس از هر تغییر مهم

---

### 🎯 لایه ۴: شبیه‌ساز تصمیم (Decision Assistant)

**چه چیزی انجام می‌دهد؟**
- شبیه‌سازی تاثیر تغییرات قبل از اعمال
- پیش‌بینی نرخ تبدیل جدید
- ارزیابی ریسک

**چگونه استفاده کنیم؟**
1. روی دکمه "🎯 شبیه‌ساز تصمیم" کلیک کنید
2. نوع تصمیم را انتخاب کنید (مثلاً تغییر قیمت)
3. مقدار فعلی را وارد کنید (مثلاً نرخ تبدیل 2.5%)
4. سطح ریسک را تنظیم کنید
5. دکمه "🚀 اجرای شبیه‌سازی" را بزنید
6. نتایج را بررسی کنید:
   - مقدار پیش‌بینی شده
   - درصد تغییر مورد انتظار
   - سطح اطمینان
   - توصیه اطلس

**زمان استفاده**: قبل از هر تصمیم مهم

**مثال شبیه‌سازی**:
```
تصمیم: تغییر قیمت محصول اصلی
مقدار فعلی: نرخ تبدیل 2.5%
سطح ریسک: 50%
نتیجه:
  مقدار پیش‌بینی: 2.65%
  تغییر: +6%
  سطح اطمینان: 85%
  توصیه: با توجه به ریسک متوسط، این تغییر مثبت است
```

---

### ⚙️ لایه ۵: تنظیمات هسته (Core Configuration)

**⚠️ فقط برای مدیران (Administrators)**

**چه چیزی تنظیم می‌کند؟**
- Auto-Index (اسکن خودکار سایت)
- بازه اسکن (هر چند وقت یکبار)
- سطح هوش اطلس
- آستانه هشدار
- نگهداری داده

**چگونه تنظیم کنیم؟**
1. روی دکمه "⚙️ تنظیمات هسته" کلیک کنید
2. تنظیمات زیر را بررسی کنید:

**Auto-Index**:
```
فعال: بله/خیر
توصیه: فقط در بازه‌های کم‌ترافیک فعال کنید
```

**بازه اسکن**:
```
مقدار: 3600 ثانیه (60 دقیقه)
حداقل: 300 ثانیه (5 دقیقه)
توصیه: برای سایت‌های پرترافیک، بازه را زیاد کنید
```

**سطح هوش**:
- 🟢 پایه: تحلیل‌های ساده و سریع
- 🔵 استاندارد: توصیه می‌شود (پیش‌فرض)
- 🟣 پیشرفته: تحلیل عمیق (نیاز به منابع بیشتر)

**آستانه هشدار**:
```
مقدار: 40
معنی: اگر امتیاز سلامت زیر 40 برود، هشدار بده
```

3. دکمه "💾 ذخیره تنظیمات" را بزنید

**زمان تنظیم**: یکبار در ابتدا، سپس در صورت نیاز

---

## سناریوهای کاربردی / Use Cases

### سناریو ۱: سایت جدید با ترافیک کم

**مشکل**: تعداد کاربران کم است

**راهکار**:
1. لایه ۱ (نمای کلان):
   - امتیاز سلامت پایین خواهد بود
   - هشدار "حجم نمونه کم" نمایش می‌دهد

2. لایه ۳ (پیشنهادات):
   - پیشنهاد "افزایش ترافیک سایت"
   - اقدامات: بهینه‌سازی SEO، کمپین تبلیغاتی

3. لایه ۵ (تنظیمات):
   - Auto-Index را غیرفعال کنید (داده کافی نیست)
   - بازه اسکن را زیاد کنید

---

### سناریو ۲: نرخ تبدیل پایین

**مشکل**: نرخ تبدیل کمتر از 2% است

**راهکار**:
1. لایه ۱ (نمای کلان):
   - متریک "نرخ تبدیل" هایلایت شده (زرد/قرمز)
   - هشدار "نرخ تبدیل پایین‌تر از حد انتظار"

2. لایه ۲ (تحلیل رفتار):
   - گلوگاه‌ها را شناسایی کنید
   - نقاط تردید کاربر را پیدا کنید

3. لایه ۳ (پیشنهادات):
   - پیشنهاد "بهبود نرخ تبدیل" با اولویت بالا
   - اقدامات مشخص برای ساده‌سازی فرم‌ها

4. لایه ۴ (شبیه‌ساز):
   - شبیه‌سازی تاثیر ساده‌سازی فرم
   - ارزیابی ریسک قبل از اعمال

---

### سناریو ۳: آمادگی برای کمپین تبلیغاتی

**مشکل**: می‌خواهید کمپین تبلیغاتی راه بیندازید

**راهکار**:
1. لایه ۱ (نمای کلان):
   - بررسی کلی سلامت سایت
   - اطمینان از آمادگی برای ترافیک بالا

2. لایه ۲ (تحلیل رفتار):
   - رفع همه گلوگاه‌های بحرانی قبل از کمپین

3. لایه ۴ (شبیه‌ساز):
   - شبیه‌سازی تاثیر افزایش ترافیک
   - پیش‌بینی نرخ تبدیل با ترافیک جدید

4. لایه ۵ (تنظیمات):
   - بازه اسکن را کم کنید برای مانیتورینگ دقیق‌تر
   - سطح هوش را روی "پیشرفته" بگذارید

---

## نکات مهم / Important Notes

### ✅ قانون ۳ کلیک
همه ویژگی‌ها حداکثر با ۳ کلیک قابل دسترسی هستند:
```
داشبورد → لایه → ویژگی
```

### 📖 قانون توضیح انسانی
هیچ عددی بدون توضیح نمایش داده نمی‌شود:
- هر متریک دارای توضیح است
- هر بینش دارای اقدام پیشنهادی است

### ⚡ کارایی
- Auto-Index باید فقط در بازه‌های کم‌ترافیک فعال باشد
- حداقل بازه اسکن: 5 دقیقه
- برای سایت‌های بزرگ، بازه را افزایش دهید

### 🔒 امنیت
- لایه ۵ فقط برای Administrators
- همه API ها نیاز به احراز هویت دارند
- Nonce verification برای همه درخواست‌ها

---

## خروجی گزارش / Report Export

### CSV Export
1. به هر لایه بروید
2. در نسخه‌های بعدی دکمه "📥 خروجی CSV" اضافه می‌شود
3. فعلاً از طریق API قابل استفاده است

### API Usage
```javascript
// Export CSV
fetch('/wp-json/homaye/v1/atlas/export/csv', {
    method: 'POST',
    headers: {
        'X-WP-Nonce': atlasConfig.nonce
    },
    body: JSON.stringify({ report_type: 'health' })
})
```

---

## عیب‌یابی / Troubleshooting

### مشکل: داشبورد بارگذاری نمی‌شود

**راه‌حل**:
1. بررسی کنید که JavaScript در مرورگر فعال است
2. Console browser را چک کنید
3. دکمه "Build" را اجرا کنید:
   ```bash
   cd plugin-directory
   npm run build
   ```

### مشکل: هیچ داده‌ای نمایش داده نمی‌شود

**راه‌حل**:
1. حداقل 7 روز داده جمع‌آوری شده باشد
2. ترافیک سایت فعال باشد
3. ردیابی در تنظیمات فعال باشد

### مشکل: نمی‌توانم به لایه ۵ دسترسی پیدا کنم

**راه‌حل**:
- فقط کاربران با نقش "Administrator" دسترسی دارند
- نقش خود را در WordPress بررسی کنید

---

## سوالات متداول / FAQ

**س: چند وقت یکبار باید اطلس را چک کنم؟**
پ: 
- لایه ۱: روزانه (30 ثانیه)
- لایه ۲ و ۳: هفتگی
- لایه ۴: قبل از تصمیمات مهم
- لایه ۵: یکبار در ابتدا

**س: آیا اطلس روی عملکرد سایت تاثیر می‌گذارد؟**
پ: خیر، اطلس فقط در پنل مدیریت اجرا می‌شود. Auto-Index در صورت فعال بودن می‌تواند بار سرور را افزایش دهد که باید در بازه‌های کم‌ترافیک اجرا شود.

**س: آیا می‌توانم پیشنهادات اطلس را خودکار اعمال کنم؟**
پ: خیر، اطلس فقط پیشنهاد می‌دهد. شما باید تصمیم بگیرید و تغییرات را اعمال کنید.

**س: حجم داده لازم برای تحلیل دقیق چقدر است؟**
پ: حداقل:
- 20 کاربر فعال در 7 روز
- 100 نشست در 30 روز
- 1000 رویداد

---

## پشتیبانی / Support

**مشکل یا سوال دارید؟**
- GitHub Issues: https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/issues
- مستندات کامل: PR9-IMPLEMENTATION.md

---

**تاریخ انتشار**: 26 دسامبر 2024
**نسخه**: 1.0.0
