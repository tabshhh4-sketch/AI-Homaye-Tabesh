# 📦 مدیریت سفارشات - Order Management Dashboard

## نمای کلی (Overview)

این سیستم یک داشبورد کامل و مدرن برای مدیریت سفارشات است که به سوپر پنل هما اضافه شده است.

## ✨ ویژگی‌های کلیدی (Key Features)

### 1. **ثبت سفارش جدید (Create New Order)**
- دکمه "➕ ثبت سفارش جدید" در بالای صفحه
- فرم جامع با بخش‌های مختلف:
  - 👤 اطلاعات مشتری
  - 📍 آدرس (صورتحساب و ارسال)
  - 📦 آیتم‌های سفارش (با قابلیت افزودن/حذف)
  - ⚙️ جزئیات سفارش (وضعیت، روش پرداخت، ارسال)
  - 📝 یادداشت‌ها

### 2. **مشاهده لیست سفارشات (Orders List)**
جدول کامل با 9 ستون:
- شماره سفارش
- مشتری
- تماس (ایمیل و تلفن)
- تاریخ
- مبلغ کل
- وضعیت (با نشان رنگی)
- روش پرداخت
- روش ارسال
- عملیات (مشاهده/ویرایش/حذف)

### 3. **جستجو و فیلتر (Search & Filter)**
- جستجو بر اساس: شماره سفارش، نام، ایمیل، تلفن
- فیلتر بر اساس وضعیت:
  - همه سفارشات
  - در انتظار
  - در حال پردازش
  - تکمیل شده
  - لغو شده

### 4. **مشاهده جزئیات (Order Details View)**
نمایش کامل اطلاعات:
- وضعیت سفارش (با نشان بزرگ رنگی)
- اطلاعات مشتری
- اطلاعات پرداخت
- اطلاعات ارسال
- لیست آیتم‌ها (جدول)
- یادداشت‌ها

### 5. **ویرایش سفارش (Edit Order)**
- کلیک روی دکمه ویرایش
- فرم با اطلاعات قبلی پر می‌شود
- امکان تغییر تمام فیلدها
- ذخیره تغییرات

### 6. **حذف سفارش (Delete Order)**
- تأییدیه قبل از حذف
- انتقال به سطل زباله (نه حذف دائمی)

## 🎨 طراحی UI/UX

### رنگ‌بندی وضعیت‌ها:
- 🟠 در انتظار پرداخت (Pending) - نارنجی
- 🔵 در حال پردازش (Processing) - آبی
- ⚪ معلق (On-Hold) - خاکستری
- 🟣 در حال آماده‌سازی (Preparing) - بنفش
- 🟢 ارسال شده (Shipped) - سبز فیروزه‌ای
- 🟢 تکمیل شده (Completed) - سبز
- 🔴 لغو شده (Cancelled) - قرمز
- 🟠 بازگشت داده شده (Refunded) - نارنجی تیره
- 🔴 ناموفق (Failed) - قرمز تیره

### المان‌های طراحی:
- دکمه‌های گرادینت با افکت hover
- کارت‌های سایه‌دار
- جداول با hover state
- فرم‌های تمیز و مرتب
- انیمیشن‌های نرم
- طراحی ریسپانسیو (موبایل، تبلت، دسکتاپ)

## 🔌 API Endpoints

### 1. لیست سفارشات
```
GET /wp-json/homaye/v1/console/orders?filter={status}
```
**پارامترها:**
- `filter`: all, pending, processing, completed, cancelled

**پاسخ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "order_number": "123",
      "customer_name": "علی محمدی",
      "customer_email": "ali@example.com",
      "customer_phone": "09123456789",
      "status": "processing",
      "total": 500000,
      "date_created": "1403/10/08 14:30",
      "payment_method": "bank",
      "shipping_method": "standard"
    }
  ]
}
```

### 2. ثبت سفارش جدید
```
POST /wp-json/homaye/v1/console/orders
```
**بدنه درخواست:**
```json
{
  "customer_name": "علی محمدی",
  "customer_email": "ali@example.com",
  "customer_phone": "09123456789",
  "billing_address": "تهران، خیابان ولیعصر",
  "shipping_address": "تهران، خیابان ولیعصر",
  "items": [
    {
      "product_name": "محصول نمونه",
      "quantity": 2,
      "price": 250000
    }
  ],
  "status": "pending",
  "payment_method": "bank",
  "shipping_method": "standard",
  "notes": "یادداشت"
}
```

**پاسخ:**
```json
{
  "success": true,
  "message": "سفارش با موفقیت ثبت شد",
  "data": {
    "id": 124,
    "order_number": "124"
  }
}
```

### 3. مشاهده جزئیات سفارش
```
GET /wp-json/homaye/v1/console/orders/123
```

### 4. بروزرسانی سفارش
```
PUT /wp-json/homaye/v1/console/orders/123
```

### 5. حذف سفارش
```
DELETE /wp-json/homaye/v1/console/orders/123
```

## 🔒 امنیت (Security)

- تمام endpoint‌ها نیاز به احراز هویت دارند
- فقط کاربران با نقش Administrator دسترسی دارند
- بررسی `manage_options` capability
- نیاز به WordPress Nonce برای درخواست‌ها

## 📋 الزامات (Requirements)

### سمت سرور:
- WordPress 5.0+
- WooCommerce 5.0+ (برای ثبت سفارشات واقعی)
- PHP 7.4+

### سمت کلاینت:
- React 18+
- مرورگر مدرن با پشتیبانی ES6

## 🚀 استفاده (Usage)

### دسترسی به داشبورد:
1. وارد پنل مدیریت WordPress شوید
2. از منوی سمت راست، گزینه "همای تابش" را انتخاب کنید
3. روی "🎛️ سوپر پنل" کلیک کنید
4. تب "📦 مدیریت سفارشات" را انتخاب کنید

### ثبت سفارش جدید:
1. روی دکمه "➕ ثبت سفارش جدید" کلیک کنید
2. اطلاعات مشتری را وارد کنید (نام و تلفن الزامی است)
3. آدرس‌ها را وارد کنید
4. آیتم‌های سفارش را اضافه کنید
5. وضعیت و روش‌های پرداخت/ارسال را انتخاب کنید
6. در صورت نیاز، یادداشت اضافه کنید
7. روی "✅ ثبت سفارش" کلیک کنید

### ویرایش سفارش:
1. در لیست سفارشات، روی آیکون ✏️ کلیک کنید
2. تغییرات مورد نظر را اعمال کنید
3. روی "💾 بروزرسانی سفارش" کلیک کنید

### مشاهده جزئیات:
1. در لیست سفارشات، روی آیکون 👁️ کلیک کنید
2. تمام جزئیات سفارش نمایش داده می‌شود

## 🐛 عیب‌یابی (Troubleshooting)

### خطا: "ووکامرس فعال نیست"
**راه‌حل:** افزونه WooCommerce را نصب و فعال کنید

### سفارش ثبت نمی‌شود
**بررسی کنید:**
- نام و شماره تماس وارد شده است؟
- حداقل یک آیتم اضافه شده است؟
- اتصال به سرور برقرار است؟

### دکمه "ثبت سفارش جدید" نمایش داده نمی‌شود
**راه‌حل:** مطمئن شوید با حساب Administrator وارد شده‌اید

## 📊 محدودیت‌ها (Limitations)

- حداکثر 100 سفارش در هر بار نمایش داده می‌شود
- برای نمایش سفارشات واقعی، WooCommerce باید نصب باشد
- فقط Administrator‌ها می‌توانند سفارش ثبت کنند

## 🔄 آپدیت‌های آینده (Future Updates)

- [ ] صدور فاکتور PDF
- [ ] ارسال ایمیل اطلاع‌رسانی به مشتری
- [ ] نمودارهای گزارش‌گیری
- [ ] صادرات CSV/Excel
- [ ] چاپ برگه سفارش
- [ ] تاریخچه تغییرات سفارش
- [ ] یکپارچگی با سیستم‌های پست و پیک

## 📝 یادداشت‌های توسعه‌دهنده

### ساختار کامپوننت:
```
OrderManagement.jsx
├── renderListView()      // نمایش جدول
├── renderForm()          // فرم ثبت/ویرایش
└── renderViewMode()      // نمایش جزئیات
```

### State Management:
```javascript
- orders: []              // لیست سفارشات
- selectedOrder: null     // سفارش انتخاب شده
- viewMode: 'list'        // حالت نمایش
- filter: 'all'           // فیلتر وضعیت
- searchTerm: ''          // عبارت جستجو
- formData: {}            // داده‌های فرم
```

### API Integration:
- استفاده از Fetch API
- مدیریت خطا با try-catch
- نمایش پیام‌های موفقیت/خطا با alert
- بارگذاری مجدد لیست پس از تغییرات

## 📞 پشتیبانی (Support)

در صورت بروز مشکل یا پیشنهاد، از طریق GitHub Issues با ما در تماس باشید.

---

**نسخه:** 1.0.0  
**تاریخ:** 1403/10/08  
**نویسنده:** Homa Development Team
