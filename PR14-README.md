# PR14: دیپلماسی هوشمند هما (Smart Diplomacy)

## 🌍 خلاصه

**Smart Diplomacy** یک سیستم پیشرفته تشخیص موقعیت جغرافیایی و ترجمه خودکار محتوا است که به افزونه همای تابش اضافه شده است. این سیستم به صورت هوشمند بازدیدکنندگان از کشورهای عربی را تشخیص داده و امکان مشاهده محتوا به زبان عربی را فراهم می‌کند.

---

## ✨ ویژگی‌های کلیدی

### 🎯 تشخیص خودکار لوکیشن (IP-Based GeoLocation)
- تشخیص کشور کاربر با استفاده از IP
- پشتیبانی از 21 کشور عربی
- Caching هوشمند برای کاهش درخواست‌های API
- دو سرویس GeoIP با Fallback

### 🔄 ترجمه داینامیک محتوا (Dynamic Translation)
- ترجمه لایه نمایش (Output Buffering)
- پشتیبانی از شورتکدها و اسلایدرها
- ترجمه محتوای WooCommerce
- ترجمه متادیتا (alt, title, placeholder)

### 💾 سیستم کشینگ هوشمند (Smart Caching)
- ذخیره ترجمه‌ها در دیتابیس
- جلوگیری از مصرف دوباره توکن Gemini
- آمارگیری و بهینه‌سازی
- Cleanup خودکار ترجمه‌های قدیمی

### 🎨 رابط کاربری حرفه‌ای (Professional UI)
- Popup خوشآمدگویی به عربی
- Language Switcher
- پشتیبانی کامل RTL
- Responsive Design

---

## 📦 نصب و راه‌اندازی

### پیش‌نیازها
- WordPress 6.0 یا بالاتر
- PHP 8.2 یا بالاتر
- افزونه همای تابش (PR1-PR13)
- Gemini API Key
- Extensions: `ext-dom`, `ext-mbstring`

### نصب
1. افزونه را فعال کنید
2. به `وردپرس → همای تابش → تنظیمات` بروید
3. Gemini API Key را وارد کنید
4. تنظیمات Smart Diplomacy را فعال کنید
5. ذخیره کنید

**راهنمای کامل**: [PR14-QUICKSTART.md](PR14-QUICKSTART.md)

---

## 🚀 نحوه استفاده

### برای کاربران عادی

1. **بازدید از کشور عربی**:
   - وارد سایت شوید
   - Popup خوشآمدگویی را مشاهده کنید
   - "نعم، ترجمة للعربية" را انتخاب کنید
   - محتوا به عربی ترجمه می‌شود

2. **تعویض دستی زبان**:
   - روی Language Switcher کلیک کنید
   - زبان تغییر می‌کند

### برای مدیران

1. **مشاهده آمار**:
   ```
   وردپرس → همای تابش → مرکز کنترل اطلس
   ```

2. **تست سیستم**:
   ```
   http://your-site.com/validate-pr14.html
   ```

3. **API Access**:
   ```bash
   GET /wp-json/homaye/v1/atlas/translation-report
   ```

---

## 📊 معماری

### ساختار کلی

```
┌─────────────────────────────────────────┐
│         بازدیدکننده سایت                │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│    HT_GeoLocation_Service                │
│    (تشخیص IP و کشور)                     │
└─────────────┬───────────────────────────┘
              │
              ▼
        ┌─────┴─────┐
        │  عربی؟    │
        └─────┬─────┘
         نه   │   بله
              ▼
┌─────────────────────────────────────────┐
│    HT_Diplomacy_Frontend                 │
│    (نمایش Popup)                         │
└─────────────┬───────────────────────────┘
              │
              ▼
        ┌─────┴─────┐
        │  انتخاب   │
        │  ترجمه؟   │
        └─────┬─────┘
              │ بله
              ▼
┌─────────────────────────────────────────┐
│    Homa_Render_Buffer_Filter             │
│    (Output Buffering & Translation)      │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│    HT_Translation_Cache_Manager          │
│    (کش و مدیریت ترجمه)                   │
└─────────────┬───────────────────────────┘
              │
        ┌─────┴─────┐
        │   Cache   │
        │   Hit?    │
        └─────┬─────┘
         نه   │   بله
              │      └──────► استفاده از Cache
              ▼
┌─────────────────────────────────────────┐
│    HT_Gemini_Client                      │
│    (ترجمه با AI)                          │
└─────────────┬───────────────────────────┘
              │
              ▼
        ذخیره در Cache
              │
              ▼
        ارسال به مرورگر
```

---

## 🗄️ دیتابیس

### جدول: `wp_homa_translations`

| ستون | نوع | توضیحات |
|------|-----|---------|
| `id` | bigint(20) | شناسه یکتا |
| `text_hash` | varchar(32) | MD5 hash برای lookup |
| `original_text` | varchar(1000) | متن اصلی (فارسی) |
| `translated_text` | text | متن ترجمه شده (عربی) |
| `lang` | varchar(5) | زبان مقصد (ar, en) |
| `is_valid` | tinyint(1) | معتبر بودن ترجمه |
| `use_count` | int(11) | تعداد استفاده |
| `created_at` | datetime | زمان ایجاد |
| `last_used` | datetime | آخرین استفاده |

**Indexes:**
- PRIMARY KEY: `id`
- UNIQUE KEY: `text_hash_lang`
- KEY: `lang`, `is_valid`, `use_count`, `last_used`

---

## 🔌 API Endpoints

### 1. Translation Report
```http
GET /wp-json/homaye/v1/atlas/translation-report
Authorization: Bearer {token}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "overview": {
      "total_cached_translations": 150,
      "total_translation_uses": 450,
      "estimated_token_savings": 15000,
      "cache_hit_rate": 66.67
    },
    "settings": {
      "translation_enabled": true,
      "monitored_countries": 21
    }
  }
}
```

### 2. Toggle Translation (AJAX)
```javascript
jQuery.post(ajaxurl, {
    action: 'homa_toggle_translation',
    action_type: 'enable', // or 'disable'
    lang: 'ar',
    nonce: homaTranslationData.nonce
});
```

---

## 🎨 Customization

### تغییر لیست کشورها

```php
add_filter('ht_arabic_countries', function($countries) {
    $countries[] = 'TR'; // اضافه کردن ترکیه
    return $countries;
});
```

### Exclude کردن بخش‌های خاص

```html
<div data-no-translate>
    این محتوا ترجمه نمی‌شود
</div>
```

### تغییر استایل Popup

```css
.homa-translation-popup {
    background: #your-color;
    border-radius: 20px;
}
```

---

## 📈 آمار و Performance

### معیارهای کلیدی

| معیار | توضیح |
|-------|-------|
| **Cache Hit Rate** | نسبت استفاده از cache به کل درخواست‌ها |
| **Token Savings** | تعداد توکن‌های صرفه‌جویی شده |
| **Translation Speed** | سرعت ترجمه (اولین بار vs بعدی) |
| **Storage Size** | حجم Cache در دیتابیس |

### بهینه‌سازی

```php
// Pre-cache محتوای مهم
$cache_manager = new HT_Translation_Cache_Manager();
$important_texts = [
    'خوش آمدید',
    'محصولات ما',
    'تماس با ما'
];
foreach ($important_texts as $text) {
    $cache_manager->get_translation($text, 'ar');
}
```

---

## 🧪 تست

### Validation Page
```
http://your-site.com/validate-pr14.html
```

**تست‌های موجود:**
- ✅ GeoIP Detection
- ✅ Translation Cache
- ✅ Database Integration
- ✅ UI Components
- ✅ Atlas Report

### Unit Tests (Future)
```bash
# Coming soon
phpunit tests/PR14Test.php
```

---

## 🛠️ عیب‌یابی

### مشکلات متداول

#### 1. Popup نمایش داده نمی‌شود
```javascript
// بررسی cookie
console.log(document.cookie);
// پاک کردن
document.cookie = "homa_translation_decided=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
```

#### 2. ترجمه کار نمی‌کند
```sql
-- بررسی API key
SELECT option_value FROM wp_options WHERE option_name = 'ht_gemini_api_key';

-- بررسی جدول
SELECT COUNT(*) FROM wp_homa_translations;
```

#### 3. Layout خراب است
```css
/* اضافه به تم */
body.homa-rtl-arabic {
    direction: rtl !important;
}
```

---

## 📚 مستندات

- **Implementation Guide**: [PR14-IMPLEMENTATION.md](PR14-IMPLEMENTATION.md)
- **Quickstart Guide**: [PR14-QUICKSTART.md](PR14-QUICKSTART.md)
- **Validation Page**: [validate-pr14.html](validate-pr14.html)

---

## 🤝 مشارکت

این PR بخشی از پروژه همای تابش است:

- **PR13**: Global Inspector
- **PR12**: Post-Purchase Automation
- **PR11**: Smart Lead Conversion
- **PR10**: Visual Guidance
- **...PR1-PR9**

---

## 📝 Changelog

### Version 1.0.0 (2025-12-26)
- ✨ تشخیص GeoIP با 2 سرویس
- ✨ سیستم ترجمه با Cache
- ✨ Popup خوشآمدگویی عربی
- ✨ Language Switcher
- ✨ Atlas Integration
- ✨ Validation Page

---

## 📄 License

GPL v3 or later

---

## 👥 Authors

- Tabshhh4 - Initial work
- GitHub Copilot - AI Assistant

---

## 🎉 تشکر

با تشکر از تیم Gemini AI و جامعه WordPress

**استفاده موفق! 🚀**
