# PR14 Quickstart Guide

## راهنمای سریع: دیپلماسی هوشمند هما

**زمان راه‌اندازی**: 5 دقیقه  
**سطح**: مبتدی تا متوسط

---

## 🚀 نصب سریع

### مرحله 1: فعال‌سازی افزونه

افزونه را فعال کنید. در زمان activation، جدول `wp_homa_translations` به صورت خودکار ایجاد می‌شود.

```bash
# بررسی جدول
mysql> USE your_database;
mysql> SHOW TABLES LIKE '%homa_translations%';
```

✅ باید جدول `wp_homa_translations` نمایش داده شود.

---

### مرحله 2: تنظیمات اولیه

1. به پنل مدیریت بروید: `وردپرس → همای تابش → تنظیمات`

2. **کلید API Gemini** را وارد کنید (ضروری برای ترجمه)
   ```
   ht_gemini_api_key: YOUR_API_KEY_HERE
   ```

3. **تنظیمات دیپلماسی هوشمند**:
   - ✅ فعال کردن ترجمه: `ht_translation_enabled = true`
   - ✅ نمایش popup: `ht_show_translation_popup = true`
   - ⬜ ترجمه خودکار: `ht_auto_translate_arabic_visitors = false` (پیشنهادی)

4. ذخیره تنظیمات

---

### مرحله 3: تست GeoIP

**الف) تست محلی (localhost):**

```php
// IP محلی به صورت خودکار به 'IR' نگاشت می‌شود
// برای تست واقعی، از VPN استفاده کنید
```

**ب) تست با VPN:**

1. VPN را به کشور عربی (مثلاً عربستان) متصل کنید
2. به سایت بروید
3. باید popup خوشآمدگویی به عربی نمایش داده شود

**ج) تست با Validation Page:**

```
http://your-site.com/validate-pr14.html
```

---

### مرحله 4: تست ترجمه دستی

برای تست بدون VPN:

```javascript
// در Console مرورگر:
document.cookie = "homa_translate_to=ar; path=/";
location.reload();
```

یا با دکمه Language Switcher در پایین صفحه.

---

## 🎯 سناریوهای استفاده

### سناریو 1: کاربر از عربستان

```
1. کاربر وارد سایت می‌شود
2. IP تشخیص داده می‌شود: SA (Saudi Arabia)
3. Popup نمایش داده می‌شود:
   "مرحباً بك! نحن نرى أنك تزورنا من السعودية"
4. کاربر کلیک می‌کند: "نعم، ترجمة للعربية"
5. صفحه reload می‌شود
6. محتوا به عربی ترجمه می‌شود
7. Layout به RTL تغییر می‌کند
```

### سناریو 2: کاربر از ایران

```
1. کاربر وارد سایت می‌شود
2. IP تشخیص داده می‌شود: IR (Iran)
3. Popup نمایش داده نمی‌شود
4. سایت به صورت عادی فارسی است
```

### سناریو 3: تعویض زبان دستی

```
1. کاربر از هر کشوری باشد
2. روی Language Switcher کلیک می‌کند
3. زبان تغییر می‌کند (FA ↔ AR)
4. محتوا ترجمه می‌شود
```

---

## 📋 Checklist تست

### ✅ تست‌های اساسی

- [ ] افزونه فعال است
- [ ] جدول `wp_homa_translations` ایجاد شده
- [ ] Gemini API key تنظیم شده
- [ ] تنظیمات ذخیره شده

### ✅ تست‌های GeoIP

- [ ] IP کاربر تشخیص داده می‌شود
- [ ] کشور صحیح است
- [ ] Popup برای کشورهای عربی نمایش داده می‌شود
- [ ] Popup برای کشورهای غیر عربی نمایش داده نمی‌شود

### ✅ تست‌های ترجمه

- [ ] متن فارسی به عربی ترجمه می‌شود
- [ ] Layout به RTL تغییر می‌کند
- [ ] فونت عربی بارگذاری می‌شود
- [ ] Shortcode ها ترجمه می‌شوند
- [ ] محتوای slider ترجمه می‌شود

### ✅ تست‌های Cache

- [ ] ترجمه در دیتابیس ذخیره می‌شود
- [ ] بار دوم از cache استفاده می‌شود
- [ ] آمار `use_count` افزایش می‌یابد

### ✅ تست‌های UI

- [ ] Popup به درستی نمایش داده می‌شود
- [ ] دکمه‌ها کار می‌کنند
- [ ] Language Switcher نمایش داده می‌شود
- [ ] انیمیشن‌ها روان هستند

---

## 🛠️ عیب‌یابی سریع

### مشکل 1: Popup نمایش داده نمی‌شود

**راه‌حل:**

```javascript
// در Console:
console.log(homaTranslationData);

// باید نمایش دهد:
// {shouldShow: true, countryCode: "SA", ...}
```

اگر `shouldShow: false`:
- Cookie `homa_translation_decided` را پاک کنید
- Cache مرورگر را پاک کنید

### مشکل 2: ترجمه کار نمی‌کند

**راه‌حل:**

```sql
-- بررسی جدول
SELECT * FROM wp_homa_translations LIMIT 10;
```

اگر خالی است:
- API key را بررسی کنید
- Error log وردپرس را بررسی کنید
- `wp-content/debug.log` را چک کنید

### مشکل 3: Layout خراب است

**راه‌حل:**

```css
/* اضافه کردن به theme CSS */
body.homa-rtl-arabic {
    direction: rtl;
}

body.homa-rtl-arabic * {
    text-align: right;
}
```

---

## 📊 نظارت و آمار

### Atlas Dashboard

برای مشاهده آمار:

```
وردپرس → همای تابش → مرکز کنترل اطلس
```

یا با API:

```bash
curl -X GET "http://your-site.com/wp-json/homaye/v1/atlas/translation-report" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**آمارهای قابل مشاهده:**
- تعداد ترجمه‌های cache شده
- تعداد استفاده از cache
- صرفه‌جویی توکن
- نرخ hit rate
- پرکاربردترین ترجمه‌ها

---

## 🎓 مثال‌های کاربردی

### مثال 1: ترجمه شورتکد Divi

```php
// شورتکد Divi
[et_pb_text]
سلام، به سایت ما خوش آمدید!
[/et_pb_text]

// پس از رندر و ترجمه:
مرحباً، أهلاً بك في موقعنا!
```

### مثال 2: ترجمه عنوان محصول WooCommerce

```php
// قبل:
<h1 class="product-title">لپ تاپ گیمینگ</h1>

// بعد:
<h1 class="product-title">لابتوب الألعاب</h1>
```

### مثال 3: ترجمه متادیتا

```html
<!-- قبل: -->
<img src="image.jpg" alt="تصویر محصول" title="مشاهده جزئیات">

<!-- بعد: -->
<img src="image.jpg" alt="صورة المنتج" title="عرض التفاصيل">
```

---

## 🔧 تنظیمات پیشرفته

### کشورهای سفارشی

```php
// در functions.php تم
add_filter('ht_arabic_countries', function($countries) {
    // اضافه کردن کشور جدید
    $countries[] = 'TR'; // ترکیه
    return $countries;
});
```

### تعویض سرویس GeoIP

```php
// در HT_GeoLocation_Service.php
private const GEOIP_SERVICE_URL = 'https://your-custom-service.com/';
```

### تنظیم Cron Cleanup

```php
// تغییر دوره cleanup از هفتگی به ماهانه
wp_clear_scheduled_hook('homa_cleanup_translation_cache');
wp_schedule_event(time(), 'monthly', 'homa_cleanup_translation_cache');
```

---

## 📱 پشتیبانی Mobile

Popup به صورت کامل Responsive است:

```css
@media (max-width: 600px) {
    .homa-translation-popup {
        width: 95%;
        margin: 20px;
    }
}
```

---

## 🌟 نکات بهینه‌سازی

### 1. Pre-cache محتوای مهم

```php
// برای صفحات پر بازدید، ترجمه را از قبل cache کنید
$cache_manager = new HT_Translation_Cache_Manager();
$cache_manager->get_translation('متن مهم', 'ar');
```

### 2. Exclude کردن بخش‌های خاص

```html
<!-- این بخش ترجمه نمی‌شود -->
<div data-no-translate>
    Technical content that should not be translated
</div>
```

### 3. افزایش سرعت با CDN

```php
// استفاده از CDN برای فونت عربی
wp_enqueue_style(
    'noto-kufi-arabic',
    'https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@300;400;600;700&display=swap'
);
```

---

## 🎉 تبریک!

شما با موفقیت Smart Diplomacy هما را راه‌اندازی کردید!

### مراحل بعدی:
1. ✅ آمارها را در Atlas بررسی کنید
2. ✅ با VPN از کشورهای مختلف تست کنید
3. ✅ ترجمه‌های cache شده را مشاهده کنید
4. ✅ ROI صرفه‌جویی توکن را محاسبه کنید

---

**سوالات؟**  
مستندات کامل: `PR14-IMPLEMENTATION.md`  
صفحه تست: `validate-pr14.html`

**موفق باشید! 🚀**
